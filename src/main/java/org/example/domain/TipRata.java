package org.example.domain;

/**
 * Represents different behavior types for ducks.
 * Replaces old classes like FlyingDuck or SwimmingDuck.
 * Values are stored in the database as strings.
 */
public enum TipRata {
    FLYING,
    SWIMMING,
    FLYING_AND_SWIMMING
}
